version: '3'

tasks:
  default:
    desc: "Generate provider documentation"
    deps:
      - :tools:install:tfplugindocs
    cmds:
      - echo "Generating provider documentation..."
      - "{{.PATH_PREFIX}} tfplugindocs"
      - echo "Provider documentation generated successfully"
