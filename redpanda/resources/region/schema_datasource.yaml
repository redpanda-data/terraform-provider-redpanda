message: "Data source for a Redpanda Cloud region"
api_schema: Region
tf_name: Region
computed_default: true

cloud_provider:
  required: true
  validator: CloudProviders

name:
  required: true

zones:

default_timezone:
  exclude: true
